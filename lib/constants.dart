import 'package:flutter/material.dart';

const kAppBackgroundColor = Colors.white;
const kBodyPadding = EdgeInsets.symmetric(horizontal: 24.0);
const kInputBodyPadding = EdgeInsets.symmetric(vertical: 18.0);
const kAppBarBackgroundColor = Colors.teal;
const kChatListViewPadding =
    EdgeInsets.symmetric(vertical: 10.0, horizontal: 20.0);
const kChatTextStyle = TextStyle(
  fontSize: 45,
  fontWeight: FontWeight.w900,
);
const kThemeTextColor = TextStyle(color: Colors.black54);
const kFocusColor = Colors.black54;
const kIsNotMeColor = Color.fromRGBO(238, 238, 238, 1);
